<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" id="svg-comm-link" width="100%" height="100%">
                <defs>
                    <linearGradient id="grad-comm-link" x1="0%" y1="0%" x2="100%" y2="100%">
                        <stop offset="0%" stop-color="#00ff41" />
                        <stop offset="100%" stop-color="#00ffff" />
                    </linearGradient>
                    <filter id="glow-comm-link" x="-30%" y="-30%" width="160%" height="160%">
                        <feGaussianBlur stdDeviation="1" result="blur" />
                        <feMerge>
                            <feMergeNode in="blur" />
                            <feMergeNode in="SourceGraphic" />
                        </feMerge>
                    </filter>
                    <style>
                        /* ИНКАПСУЛИРОВАННАЯ ЛОГИКА ДВИЖЕНИЯ */
                        @keyframes spin-comm-link { 100% { transform: rotate(360deg); } }
                        @keyframes rev-spin-comm-link { 100% { transform: rotate(-360deg); } }
                        @keyframes pulse-comm-link { 0%, 100% { transform: scale(1); filter: brightness(1); } 50% { transform: scale(1.08); filter: brightness(1.3); } }
                        @keyframes pulse-fast-comm-link { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.15); filter: brightness(1.5); } }
                        @keyframes float-comm-link { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-4px); } }
                        @keyframes glitch-comm-link { 0%, 90%, 100% { transform: translate(0,0) skew(0deg); stroke: url(#grad-comm-link); } 92% { transform: translate(-2px, 1px) skew(-5deg); stroke: #ff003c; } 96% { transform: translate(2px, -1px) skew(5deg); stroke: #00ffff; } }
                        @keyframes glitch-heavy-comm-link { 0%, 80%, 100% { transform: translate(0,0) scale(1); stroke: url(#grad-comm-link); } 82% { transform: translate(-4px, 2px) scale(1.1); stroke: #ff003c; } 86% { transform: translate(4px, -2px) scale(0.9); stroke: #00ffff; } 88% { stroke: #ffffff; } }
                        @keyframes draw-comm-link { 0% { stroke-dashoffset: 100; } 50% { stroke-dashoffset: 0; } 100% { stroke-dashoffset: -100; } }
                        @keyframes breathe-comm-link { 0%, 100% { opacity: 0.6; stroke-width: 1; } 50% { opacity: 1; stroke-width: 1.8; filter: drop-shadow(0 0 5px #00ff41); } }
                        @keyframes blink-comm-link { 0%, 50% { opacity: 1; stroke: #ff003c; } 51%, 100% { opacity: 0.3; stroke: url(#grad-comm-link); } }
                        @keyframes wave-comm-link { 0%, 100% { transform: scaleY(1); } 50% { transform: scaleY(1.3); } }
                        @keyframes shake-comm-link { 0%, 80%, 100% { transform: rotate(0deg); } 85% { transform: rotate(-8deg); } 90% { transform: rotate(8deg); } 95% { transform: rotate(-8deg); } }

                        #svg-comm-link .hud-spin { transform-origin: 12px 12px; animation: spin-comm-link 30s linear infinite; }
                        #svg-comm-link .core-anim { transform-origin: 12px 12px; stroke-dasharray: 100; animation: draw-comm-link 4s linear infinite; }
                    </style>
                </defs>
                <!-- Tactical HUD Grid (Фоновый слой сканирования) -->
                <g opacity="0.3" stroke="#00ffff" stroke-width="0.3" fill="none">
                    <g class="hud-spin">
                        <circle cx="12" cy="12" r="10" stroke-dasharray="1 4" />
                        <line x1="12" y1="0" x2="12" y2="24" stroke-dasharray="2 8" opacity="0.3" />
                        <line x1="0" y1="12" x2="24" y2="12" stroke-dasharray="2 8" opacity="0.3" />
                    </g>
                    <!-- Статичные углы калибровки -->
                    <path d="M 2 2 L 4 2 M 22 2 L 20 2 M 2 22 L 4 22 M 22 22 L 20 22 M 2 2 L 2 4 M 22 2 L 22 4 M 2 22 L 2 20 M 22 22 L 22 20" />
                </g>
                <!-- Основной динамический контур -->
                <g stroke="url(#grad-comm-link)" stroke-width="1.2" fill="none" stroke-linecap="square" stroke-linejoin="miter" class="core-anim" filter="url(#glow-comm-link)">
                    <polygon points="2,4 22,4 22,16 14,16 8,22 8,16 2,16"/><line x1="6" y1="8" x2="18" y2="8"/><line x1="6" y1="12" x2="14" y2="12"/>
                </g>
            </svg>